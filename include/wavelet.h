#ifndef WAVELET_H
#define WAVELET_H

#include <vector>

// Function to apply wavelet transform to a time series data vector
std::vector<double> applyWaveletTransform(const std::vector<double>& data);

#endif